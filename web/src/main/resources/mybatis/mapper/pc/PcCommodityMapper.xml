<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="PcCommodityMapper">
    <select id="selectCommodityIntroduction" resultType="cn.ykthink.jewelry.model.pc.user.vo.PcUserCommodityIntroductionVO">
        select commodity.uuid,image_url,title,subhead,commodity_price
        from commodity,images
        where commodity.uuid = images.commodity_uuid
            and commodity.is_deleted = 0
        order by commodity.create_time
    </select>
</mapper>